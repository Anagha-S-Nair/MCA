#Create a class Person (name, age) with a constructor and a method display(). Create a class Employee (EmpID) from Person, override display() to include the ID. 
# Create a derived class Faculty (department) from Employee, override display() to include the department. Create a separate class Researcher with a method can_do_research() that returns a string indicating the person can conduct research. 
# Create a final class Professor which inherits from Faculty and Researcher.
